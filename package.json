{
  "name": "BlucyPortfolio",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^2.1.1",
    "nunjucks": "^3.2.1",
    "jimp": "^0.11.0",
    "core-js": "^3.6.5"
  },
  "scripts": {
    "build": "rm -rf build && mkdir build && cp CNAME build && cp -r src/public build && node src/builder.js",
    "deploy": "gh-pages -d build -b master"
  },
  "homepage": "https://blucyna.github.io/portfolio"
}
